package com.bsm.controll;

import java.util.Map;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;

import jakarta.servlet.http.HttpServletRequest;

@Controller
public class ShowHomeController {
	/*
	 * @GetMapping("/process") public String process() {
	 * System.out.println("ShowHomeController.process()"); return "forward:report";
	 * }
	 * 
	 * @GetMapping("/report") public String showReport(Map<String,Object> map) {
	 * System.out.println("ShowHomeController.showReport()"); return "show_data"; }
	 */

	/*
	 * @RequestMapping("/process") public String process(HttpServletRequest req) {
	 * System.out.println("ShowHomeController.process()"+req.hashCode()); return
	 * "redirect:report"; }
	 * 
	 * @RequestMapping("/report") public String showReport(HttpServletRequest req) {
	 * System.out.println("ShowHomeController.showReport()"+req.getAttribute("attr1"
	 * )); return "show_data"; }
	 */
	
	 
}
